# marker-funeq

Informal claim:

> Two candidates for the Relativizing funeq/funne marker semantics: (slot 1) the as-implemented unconditional-atom reading — function-level equality p = q relativizes unconditionally, no per-fiber markers; (slot 2) the as-#1-requested pointwise-propagation reading — function-level equality p = q relativizes only when every fiber p x, q x carries a Relativizing marker. Run both readings as parallel candidate artifacts through the campaign machinery and mine any disagreement into one plain-language intent probe.
